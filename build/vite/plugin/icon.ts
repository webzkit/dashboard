/**
 * On-demand config icon
 * https://github.com/antfu/unplugin-icons
 */
import Icons from 'unplugin-icons/vite';

export function configIconPlugin() {
  return Icons({
    autoInstall: true,
    scale: 1.2,
  });
}
